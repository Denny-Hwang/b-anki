{
  "name": "b-anki",
  "version": "1.0.0",
  "private": true,
  "description": "Static GitHub Pages front-end for B-Anki. The Python package under banki/ stays the reference implementation for the learning algorithms.",
  "scripts": {
    "build": "python3 scripts/build_site.py",
    "serve": "python3 scripts/build_site.py && python3 -m http.server -d _site 8123",
    "test": "npm run test:logic && npm run test:e2e",
    "test:logic": "python3 scripts/gen_fixtures.py && node web/tests/logic.test.mjs",
    "test:e2e": "node web/tests/e2e.test.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
